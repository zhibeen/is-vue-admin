---
alwaysApply: true
---

# IS-Vue-Admin 项目开发规范总览

**重要提示**: 本规范已拆分为4个独立规则文件，请根据工作场景参考对应规范。

## 🎯 核心原则

### 1. 不要过度文档化
- **常规开发**: 直接在响应中说明做了什么，无需创建文档
- **Bug 修复**: 简要说明原因和解决方案即可
- **重大变更**: 询问用户是否需要创建详细文档

### 2. 代码质量优先
- **前端**: 使用 VXE Table 新版语法、正确导入 Ant Design 组件
- **后端**: 遵循分层架构、注意返回值包装、防止 N+1 查询
- **测试**: 代码提交前确保可以正常运行

### 3. Git 安全原则
- **不主动提交**: AI 提供建议，由用户决策
- **不强制推送**: 特别是 main/master 分支
- **清晰提交信息**: 遵循 Conventional Commits 规范

## 📂 详细规范索引

### → 前端开发
**文件**: `.cursor/rules/frontend.mdc`
- VXE Table v4.17+ 最新语法
- Ant Design Vue 组件导入
- Tailwind CSS 和深色模式
- API 调用规范

### → 后端开发
**文件**: `.cursor/rules/backend.mdc`
- 分层架构（Route → Service → Model）
- API 返回值规范
- SQLAlchemy 2.0 写法
- Alembic 数据迁移
- Docker 命令执行

### → 文档记录
**文件**: `.cursor/rules/documentation.mdc`
- 何时创建/不创建文档
- 文档格式规范
- 代码注释规范
- API 文档规范

### → Git 工作流
**文件**: `.cursor/rules/git.mdc`
- 分支管理策略
- Commit Message 规范
- AI 提交行为规范
- 冲突解决策略

## 🚀 项目架构速览

### 技术栈
- **前端**: Vue 3 + TypeScript + Vben Admin 5.0 + Ant Design Vue
- **后端**: Python 3.10+ + APIFlask + SQLAlchemy 2.0 + PostgreSQL
- **认证**: JWT (Bearer Token)
- **部署**: Docker Compose (后端) + 本地 Node (前端)

### 统一响应格式
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

### 目录结构
```
is-vue-admin/
├── backend/          # Python/APIFlask 后端
│   ├── app/
│   │   ├── api/      # API 路由
│   │   ├── models/   # SQLAlchemy Models
│   │   ├── services/ # 业务逻辑层
│   │   └── schemas/  # 数据验证
│   └── tests/        # 自动化测试
├── frontend/         # Vue 3 前端 (Vben Admin)
│   └── apps/web-antd/
└── .cursor/rules/    # 开发规范（本文件）
```

## ⚡ 快速参考

### 后端关键规范
```python
# ✅ 正确的返回值
return {'data': {'token': '...'}}  # 返回字典
return user_model                  # 返回 Model
return {'data': [item1, item2]}    # 返回列表

# ✅ 防止 N+1 查询
stmt = select(Product).options(
    selectinload(Product.category)
)

# ✅ 数据迁移（项目根目录执行）
docker compose exec backend flask db migrate -m "描述"
docker compose exec backend flask db upgrade
```

### 前端关键规范
```vue
<script setup lang="ts">
// ✅ 导入 Ant Design 组件
import { Button, Card, Tag } from 'ant-design-vue';

// ✅ VXE Table 新版语法
const gridOptions = {
  toolbarConfig: {
    refresh: true,
    refreshOptions: { code: 'query' }
  }
}

// ✅ API 调用
import { requestClient } from '#/api/request';
</script>
```

### Git 提交规范
```bash
# ✅ 好的提交信息
git commit -m "feat(product): 添加商品搜索功能"
git commit -m "fix(frontend): 修复 VXE Table 刷新按钮问题"

# ❌ 差的提交信息
git commit -m "update"
git commit -m "fix bug"
```

## 💡 AI 工作指南

### 前端开发时
1. 检查 VXE Table 是否使用新版语法
2. 检查 Ant Design 组件是否正确导入
3. 确保样式支持深色模式（使用 Tailwind + Ant Design 组件）

### 后端开发时
1. 业务逻辑必须在 Service 层，不要写在 routes.py
2. 注意返回值包装规则（字典包装在 data 中）
3. 使用 selectinload/joinedload 防止 N+1 查询
4. 数据库变更必须通过 Alembic

### 完成任务后
1. **不要主动创建文档**（除非用户明确要求）
2. 在响应中简洁说明：做了什么、如何验证、注意事项
3. 代码注释 > 内联说明 > 独立文档

### 代码修改完成
1. **不要主动提交代码**
2. 提供符合规范的提交信息建议
3. 建议分批提交策略（前端/后端分开）

## 🔗 相关文档

- **技术框架完整文档**: `开发设计文档/总则/技术框架.md` (752行)
- **项目启动指南**: `README.md`
- **实施路线图**: `开发设计文档/总则/SERC财务中台实施路线图.md`

---

**规范版本**: v2.0  
**最后更新**: 2025-01-20  
**维护者**: 项目开发团队

**注**: 本文件设置了 `alwaysApply: true`，将始终应用于所有 AI 对话。
